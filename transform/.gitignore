# dbt artifacts (also ignored at repo root)
target/
dbt_packages/
dbt_internal_packages/
logs/
.user.yml
.venv/

# Local profiles / env — never commit keys or a real profiles.yml
profiles.yml
.env
.secrets/
*.json
!package-lock.yml
