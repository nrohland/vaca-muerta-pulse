{{ config(materialized="view") }}

select
    {{ dbt_utils.generate_surrogate_key(["idpozo", "anio", "mes"]) }} as well_month_id,
    idpozo,
    sigla,
    anio,
    mes,
    periodo,
    {{ cap4_days_in_month("periodo") }} as days_in_month,
    prod_pet_m3,
    prod_gas_km3,
    prod_agua_m3,
    tef,
    empresa,
    idempresa,
    formacion,
    formprod,
    tipo_de_recurso,
    sub_tipo_recurso,
    cuenca,
    provincia,
    areapermisoconcesion,
    idareapermisoconcesion,
    areayacimiento,
    idareayacimiento,
    tipopozo,
    tipoestado,
    tipoextraccion,
    profundidad,
    rectificado,
    habilitado,
    fechaingreso,
    fecha_data,
    source_batched_at,
    source_extracted_at
from {{ ref("stg_produccion_pozo_mes") }}
